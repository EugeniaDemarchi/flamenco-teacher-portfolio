export {};
/*

const parametros = new URLSearchParams(window.location.search);


const slug =parametros.get("slug");

const encontrado = parametros.find((slug)=>  === ) */
//# sourceMappingURL=render-obra-detalle.js.map